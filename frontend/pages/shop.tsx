import React from "react";
import Card from "components/cards/card";
type Props = {};

const shop = (props: Props) => {
  return (
    <div>
      <Card></Card>
    </div>
  );
};

export default shop;
