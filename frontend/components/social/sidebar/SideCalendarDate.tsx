import { Card, CardContent } from "@mui/material";
import React from "react";

type Props = {};

const SideCalendarDate = (props: Props) => {
  return (
    <div className="mt-2">
      <Card>
        <CardContent></CardContent>
      </Card>
    </div>
  );
};

export default SideCalendarDate;
